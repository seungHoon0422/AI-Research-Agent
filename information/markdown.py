# Markdown contents centralized

DOMAIN_REGISTER_GUIDE_MD = """
ℹ️ 기능 명세:
- 사용자가 관심 도메인(URL)을 최대 3개까지 등록할 수 있습니다.
- 등록된 도메인은 등록되어있는 도구(tavily, paper-search ..) 를 통해 검색하는데 활용됩니다.
- 중복 URL 방지, 형식 검증(http/https), 입력칸 동적 추가를 지원합니다.

ℹ️ 동작 방식:
- 올바른 URL만 검색 도메인 목록에 저장되며, 등록 성공/오류는 토스트로 안내됩니다.
- '+' 버튼으로 입력칸을 추가할 수 있고, CLEAR 버튼으로 저장된 도메인을 초기화할 수 있습니다.
- 입력칸에 도메인(URL)을 입력 후 '도메인 등록'을 클릭하면`유효성 검증` 및 `프롬프트 업데이트`를 수행합니다.
- 등록된 도메인 목록은 시스템 프롬프트에 반영되어 `도메인 기반 검색/요약의 컨텍스트로 사용`됩니다.
"""

DOC_UPLOAD_GUIDE_MD = """
ℹ️ 기능 명세:
- 관심있는 기술 문서를 업로드하여 업로드한 문서를 기반으로 AI Search를 통해 검색 가능한 상태로 등록합니다.
- Document Hub 등록 이후 다음 페이지에서 업로드한 문서 기반 채팅이 가능합니다.

ℹ️ 동작 방식:
- 파일 업로드 : `Browse files` 버튼 클릭, `Drag and Drop` 방식으로 문서 업로드 가능
- 파일 미리보기 : `문서 미리보기` 버튼 클릭, 문서 미리보기 팝업 창 표시
- 문서 파일 업로드 이후 `파일 업로드 버튼 클릭` 시, `Azure Blob Storage`에 자동으로 문서 업로드
"""

DOC_UPLOAD_INFO_MD = """
ℹ️ 업로드 가능한 파일 형식:
- 업로드 가능한 파일 형식: **pdf, docx, doc, txt, md**
- 파일은 최대 5개까지 업로드 가능합니다.
- 업로드가 완료된 문서는 아래에 표시됩니다.
"""

DOC_HUB_GUIDE_MD = """
ℹ️ 기능 명세:
- 업로드한 문서를 Azure Blob Storage에 저장하고, Azure AI Search Indexer로 색인합니다.
- 'Document Hub 업데이트'로 인덱서를 실행하여 인덱스를 갱신합니다.
- 'Document Hub 연결 확인'으로 인덱서 상태(최근 실행, 상태, 오류)를 조회합니다.

ℹ️ 동작 방식:
- 파일 업로드: 상단 '파일 업로드'에서 문서를 선택 후 업로드하면 Blob Storage에 저장됩니다.
- 색인 갱신: 'Document Hub 업데이트' 클릭 시 `IndexerManager.run_indexer()`를 호출해 인덱서를 즉시 실행합니다.
- 상태 조회: 'Document Hub 연결 확인' 클릭 시 `IndexerManager.get_indexer_status()`로 현재/최근 실행 상태를 표시합니다.
- 성공/실패 결과는 success/error 메시지 및 토스트로 피드백되며, 상세 응답은 JSON으로 확인할 수 있습니다.
"""

GUIDE_01_SUMMARY_MD = """
- **도메인 관리**: 관심 도메인 등록/초기화로 수집 범위 설정
- **문서 등록**: 기술 문서 업로드를 통해 검색 데이터 추가 및 미리보기
- **색인 실행**: AI Search Document로 등록 반영해 검색 가능 상태 전환
"""

GUIDE_02_SUMMARY_MD = """
- **RAG 질의응답**: 업로드한 Document 기반 답변 생성
- **도구 호출**: 외부 도구 결과와 모델 답변을 구분 표시 및 MCP 도구를 활용해 최신 검색 결과 반영
- **인용 표시**: 답변에 사용된 문서 청크 데이터, 도메인 출처 표시
"""

GUIDE_03_SUMMARY_MD = """
- **지식 그래프**: [Document Hub] - [카테고리] - [키워드] 관계 시각화
- **카테고리 필터**: 선택된 카테고리 항목만 그래프/목록 반영
- **문서 목록**: 카테고리 별 Document Hub에 등록된 문서 목록 표시 및 페이지네이션
"""


